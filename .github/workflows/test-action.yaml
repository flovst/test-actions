name: test-actions
run-name: ${{ github.actor }} is learning GitHub Actions
on: 
  # push:
  workflow_dispatch:
jobs:
  check-bats-version:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - run: npm install -g bats
      - run: bats -v

  test01:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: pwd
      - name: Testing Env 
        env:
          TEST_ENV: test_env01
          T_S: ${{ secrets.TEST_SECRET }}
          T_V: ${{ vars.TEST_VARIABLE }}
        run: echo "${{ TEST_ENV }}"  
        # run: echo "${{ secrets.TEST_SECRET }}"

      - run: pwd
      - run: echo "${{ TEST_ENV }}"  
      - run: echo "${{ secrets.TEST_SECRET }}"
      # - run: |
      #   pwd
      #   ls
      #   printf "${{secrets.TEST_SECRET}}"
      #   printf "${{vars.TEST_VARIABLE}}"
      #   printf "${{T_S}}"
      #   printf "${{T_V}}"
